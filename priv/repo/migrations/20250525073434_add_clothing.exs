defmodule Resdayn.Repo.Migrations.AddClothing do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    create table(:clothing, primary_key: false) do
      add(:id, :text, null: false, primary_key: true)
      add(:name, :text)
      add(:type, :text, null: false)
      add(:value, :bigint, null: false)
      add(:weight, :decimal, null: false)
      add(:nif_model_filename, :text, null: false)
      add(:icon_filename, :text)
      add(:enchantment_points, :bigint, null: false)
      add(:body_part_coverings, {:array, :map}, default: [])
      add(:flags, {:array, :text}, null: false, default: [])

      add(
        :script_id,
        references(:scripts,
          column: :id,
          name: "clothing_script_id_fkey",
          type: :text,
          prefix: "public"
        )
      )

      add(
        :enchantment_id,
        references(:enchantments,
          column: :id,
          name: "clothing_enchantment_id_fkey",
          type: :text,
          prefix: "public"
        )
      )
    end
  end

  def down do
    drop(constraint(:clothing, "clothing_script_id_fkey"))

    drop(constraint(:clothing, "clothing_enchantment_id_fkey"))

    drop(table(:clothing))
  end
end
